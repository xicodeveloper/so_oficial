#include "header.h"

//O Servidor deverá ser lançado tendo como parâmetro o ficheiro de configuração. Os dados
//no ficheiro de configuração que deverão estar presentes, no mínimo, para o Servidor são os
//seguintes: ficheiro de jogos e de soluções (podem ser dois ficheiros).

//• enviaJogo(IdCliente, idJogo, Tabuleiro)
//• recebeSolucao(idCliente, idJogo, Solucao)
//• respondeSolucao(idCliente, idJogo, "Certo/Errado"